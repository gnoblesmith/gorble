const TopBar = () => {

    const style = {
        flex: "0 1 auto",
        width: "100%", 
        textAlign: "center",
        fontSize: '60px',
        fontFamily: "Arial, sans-serif",
        borderBottom: "2px solid grey"
    };

    return <div style={style}>
        Gorble
    </div>
}

export default TopBar;